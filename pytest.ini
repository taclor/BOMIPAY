[pytest]
addopts = -q
asyncio_mode = auto
markers =
    staging: staging tests using mock HTTP server (no real API keys required)
    performance: endpoint latency and throughput tests (use -m performance to run)
